services:
  tinyauth:
    image: ghcr.io/tinyauthapp/tinyauth:v5
    container_name: tinyauth
    restart: unless-stopped
    expose:
      - 3000
    environment:
      TZ: ${TZ}
      TINYAUTH_APPURL: https://${TINYAUTH_HOSTNAME?}
      TINYAUTH_SECRET: ${TINYAUTH_SECRET?}
      TINYAUTH_AUTH_USERS: ${TINYAUTH_AUTH_USERS?}
      TINYAUTH_SERVER_PORT: 3000
      TINYAUTH_SERVER_ADDRESS: 0.0.0.0
      TINYAUTH_AUTH_SECURECOOKIE: true
      TINYAUTH_AUTH_SESSIONEXPIRY: 604800
      TINYAUTH_DATABASE_PATH: /data/tinyauth.db
      TINYAUTH_OAUTH_PROVIDERS_POCKETID_CLIENTID: ${POCKET_ID_TINYAUTH_CLIENTID?}
      TINYAUTH_OAUTH_PROVIDERS_POCKETID_CLIENTSECRET: ${POCKET_ID_TINYAUTH_CLIENTSECRET?}
      TINYAUTH_OAUTH_PROVIDERS_POCKETID_AUTHURL: https://${POCKET_ID_HOSTNAME?}/authorize
      TINYAUTH_OAUTH_PROVIDERS_POCKETID_TOKENURL: https://${POCKET_ID_HOSTNAME?}/api/oidc/token
      TINYAUTH_OAUTH_PROVIDERS_POCKETID_USERINFOURL: https://${POCKET_ID_HOSTNAME?}/api/oidc/userinfo
      TINYAUTH_OAUTH_PROVIDERS_POCKETID_REDIRECTURL: https://${TINYAUTH_HOSTNAME?}/api/oauth/callback/pocketid
      TINYAUTH_OAUTH_PROVIDERS_POCKETID_SCOPES: openid email profile groups
      TINYAUTH_OAUTH_AUTOREDIRECT: pocketid
      TINYAUTH_AUTH_TRUSTEDPROXIES: 172.18.0.0/16
      # These paths are publicly reachable by Stremio clients and cannot require a session cookie.
      # Adjust the regexes if you rename services or change their URL structure.
      TINYAUTH_APPS_aiostreams_CONFIG_DOMAIN: ${AIOSTREAMS_HOSTNAME?}
      TINYAUTH_APPS_aiostreams_PATH_ALLOW: ^/stremio/|^/api/v1/usenet/stream/|^/api/v1/debrid/playback/
      TINYAUTH_APPS_seedstream_CONFIG_DOMAIN: ${SEEDSTREAM_HOSTNAME?}
      TINYAUTH_APPS_seedstream_PATH_ALLOW: ^/health|^/[^/]+/(manifest\.json|stream/|play/|next/)
      TINYAUTH_APPS_aiometadata_CONFIG_DOMAIN: ${AIOMETADATA_HOSTNAME?}
      TINYAUTH_APPS_aiometadata_PATH_ALLOW: ^/(manifest\.json|stream/|catalog/|meta/)|^/stremio/[^/]+/(manifest\.json|stream/|catalog/|meta/)
      TINYAUTH_APPS_stremthru_CONFIG_DOMAIN: ${STREMTHRU_HOSTNAME?}
      TINYAUTH_APPS_stremthru_PATH_ALLOW: ^/(v0/)?proxy/|^/stremio/[^/]+/(manifest\.json|stream/|catalog/|meta/)|^/stremio/addon_catalog/
    volumes:
      - ${DOCKER_DATA_DIR}/tinyauth:/data
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tinyauth.rule=Host(`${TINYAUTH_HOSTNAME?}`)"
      - "traefik.http.routers.tinyauth.entrypoints=websecure"
      - "traefik.http.routers.tinyauth.tls.certresolver=letsencrypt"
      - "traefik.http.services.tinyauth.loadbalancer.server.port=3000"
      - "traefik.http.middlewares.tinyauth.forwardauth.address=http://tinyauth:3000/api/auth/traefik"
      - "traefik.http.middlewares.tinyauth.forwardauth.trustForwardHeader=true"
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:3000/ || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    profiles:
      - tinyauth
      - required
      - all
